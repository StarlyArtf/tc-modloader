# 电路封装元件验收（2026-09-16，步骤 1）

- 新增电路格式分析工具 `tools/circuit_format.py`，可解码指定构建的 v13/v14
  `circuit.data`：文件首字节为版本，其余为原始 Snappy 数据；解析器和编码器均保留
  未命名字段，并区分 v13 与 v14 的组件字段布局。
- 新增无第三方压缩依赖的 C++ fixture 生成器和真实游戏探针：
  `tests/and-component-fixture.cpp`、`tests/and-component-probe.cpp`。
- 生成的两输入一输出 AND 元件固定 ID 为 `0x414E44325F303031`。隔离游戏实测导入成功，
  返回同一 64 位 ID；原型名称为 `AND2 Test`，输入数为 2、输出数为 1，三个引脚的
  raw word size 均为 1。所有权快照释放后 Nim 错误状态仍为 0。
- 该测试验证了电路文件导入、64 位 ID、自定义引脚数量和一位位宽。菜单位置、拖动放置、
  真值表仿真、保存重载和有状态行为尚未验收。
- 证据：`build/and-component-playtest-d5eb786ded044ff08b8ae278cbbc2ec9/game/tc-modloader-data/loader.log`。

# 元件菜单与放置验收（2026-09-16，步骤 2）

- 新增 `tests/component-placement-probe.cpp`，在隔离游戏中导入 AND fixture，进入 sandbox，
  调用组件菜单使用的同一 `add_component` helper，把自定义 kind `0x4e` 和 64 位 ID 放入
  菜单放置模板。
- 实测 helper 返回成功，board 组件数从 1 增至 2；随后按 board 组件数组的实际布局
  `payload + 8 + index * 0x238` 扫描，找到 kind `0x4e`、位置 `(30,0)`、ID
  `0x414E44325F303031` 的实例。
- 这表明自定义元件已进入可被菜单放置的原型表，并能通过菜单模型路径落到 board。
  尚缺的是纯视觉上的“打开列表并按下鼠标拖动”自动化；本轮没有把模型放置冒充鼠标操作。
- 证据：`build/component-placement-playtest-80554655e4a74f4da93ce23f542f1b89/game/tc-modloader-data/loader.log`。

# 元件连线与真值表验收（2026-09-16，步骤 3）

- 新增 `tests/and-component-netlist.js`：解码生成的 AND fixture，按游戏组件引脚偏移解析
  `0x4f` 输入引脚、`0x51` 输出引脚和 `0x04` AND 门，建立导线网络并逐一评估
  `00、01、10、11`。结果为 `0、0、0、1`，测试通过。
- 该测试发现并修正了 fixture 中两段导线的方向位：方向 2（南）应为 `0x4000`，
  方向 6（北）应为 `0xC000`；原先写成了 `0x2000`／`0x6000`。修正后游戏导入、
  菜单放置和真值表三组测试均再次通过。
- 真实游戏侧已通过：导入返回 2 输入、1 输出、1 位宽；sandbox 菜单 helper 放置成功。
  当前真值表是对 fixture 内部网表的确定性评估；使用游戏自身关卡测试循环读取每个
  周期的真实输出仍待补做。
- 证据：`node tests/and-component-netlist.js build/and2_component.data` 输出
  `PASS AND fixture netlist: 00, 01, 10, 11 -> 0, 0, 0, 1`。

# 元件导入封装验证（2026-09-16，第二阶段）

- 新组件模型测试通过：二进制字符串含 NUL、64 位返回 ID、临时分配释放平衡、目录参数、错线程拒绝、调用前后 Nim 错误状态及快照释放。
- 修复 `rawNewString` 容量与逻辑长度混淆，旧 SDK 模型测试替身同步纠正，回归通过。
- 两次独立游戏安装／USERPROFILE 实测通过：字节导入、同 ID 目录更新、名称和长度、getter 快照释放、缺失目录返回拒绝；最后一次额外验证真实字符串 setter。
- 真实 EXE 静态检查增加至 29 项并通过。
- 未部署至玩家安装。测试电路导入后无自定义 I/O 端口；完整菜单／放置／仿真仍未验证。详见 [研究记录第二阶段](research/COMPONENT-PIPELINE.md#6-第二阶段导入封装与真实游戏验证)。

# 元件链路研究验证（2026-09-16，第一阶段）

- `node tools/Inspect-Components.js`：26 项指定 EXE 静态检查通过，涵盖 64 位自定义 ID、注册调用链、代码生成、动态编译入口和 JIT 执行。
- SDK 模型回归重新编译并通过；自定义 ID 使用高位非零的 `0x123456789abc007b`，检查 lookup key 的 8 字节 ID 布局。
- 这是静态二进制验证和替身模型测试，不是游戏内新元件功能验证。本轮未运行游戏、未修改玩家存档，也未部署新的加载器或插件。
- 复现步骤及尚待验证的内容见 [元件链路研究](research/COMPONENT-PIPELINE.md)。

# 0.3.0 存档隔离验证

- 已从安装的正式 0.2.0 成功升级到 0.3.0。
- 新增存档测试通过：Unicode 文件名、二进制电路原样复制；云元数据排除；重复导入生成不同副本；原版、默认 Mod 存档及旧导入副本保留；符号链接／目录联接源被拒绝；失败不切换；非法 profile 路径被拒绝。
- 原有 25 项核心测试、6 项安装／卸载测试，以及原生 Hook、篡改拒绝、冲突和停用测试全部再次通过。
- 实际游戏启动测试使用独立 USERPROFILE，故意设置不同 APPDATA。新目录 default 中实际生成 levels.txt、settings.txt 与其他游戏文件；日志确认早期存档重定向成功。
- 游戏运行期间通过与 UI 相同的导入后端导入原版副本，重启后日志确认切换到 import-* 目录；Mods 管理页正常打开。
- 原版测试源的 464 个文件 SHA-256 在两次游戏运行和导入后全部一致。
- 最终安装保留默认存档选择，未自动导入或修改用户真正的原版存档。

边界：未做 Steam 云服务联网行为测试，未逐个电路完成关卡验证。任意原生插件可自行绕开游戏接口，存档隔离不是操作系统沙箱。不同 Mod 组合目前共用所选副本，没有自动按组合分档。导入前需关闭原版游戏以获得稳定副本。

以下为此前版本验证记录。

# 0.2.0 验证记录

验证环境：2026-09-15，Windows x64，本地《Turing Complete》2.1.334。EXE／引擎哈希见 README。

## 自动验证（正式构建）

- 25 项文件管理集成测试通过：部署、重复应用、恢复、新文件删除、依赖、文件冲突、大小写冲突、重复 ID、缺失包、损坏包、非法路径、文本补丁、外部修改保护、事务中断恢复。
- 6 项安装器测试通过：安装和原引擎备份、重复安装、卸载精确恢复、重复卸载、不兼容 EXE 拒绝、不兼容引擎拒绝。
- 原生宿主测试使用正式示例 DLL、真实 MinHook 和带 COFF 符号的测试 EXE：目标 100000 被改为当前 100 + 100 = 200；较短目标 150、暂停及重置命令保持原值。
- 替换已应用的包后，指纹不匹配阻止执行。
- 第二个插件 Hook 相同函数时初始化失败，第一个插件仍正常工作。
- 已启用插件经全部停用后，在新的运行时中不会初始化或拦截命令。

原生宿主测试的仿真函数是测试替身，以下实机验证另行记录。

## 实机验证

- 主菜单 Mods 入口、中文管理页、启用／应用／停用资源示例以及关闭管理页后进入原有选项页，已在此前 0.1.0 实机验证；0.2.0 沿用该入口，增加原生包状态和说明。
- 0.2.0 实际游戏进程成功加载独立原生 DLL，安装 sim_do Hook 并绘制中文周期守卫面板。
- 开发测试插件向真实游戏的已初始化仿真内核提交连续运行目标 INT64_MAX。Hook 将初始周期 -1 的目标改成 99；实际读取周期达到 99 并持续至少 1 秒，记录 SELFTEST PASS。
- 上述实机内核验证在主菜单阶段完成，不能等同于完整关卡／战役流程测试。另一次自动进入关卡的尝试未完成，不计为通过。
- 更正：0.2.0 测试只设置 APPDATA，后续反汇编发现游戏依据 USERPROFILE 拼接路径，因此旧测试不能视为存档已隔离。0.3.0 按实际路径重新验证。
- 自动测试宏 TC_GUARD_SELFTEST 不用于正式 DLL 构建。正式示例不会自动点击菜单或自动开始仿真。
- 最终正式安装再次启动成功：日志确认主菜单入口已绘制、示例加载成功且仅注册 1 个仿真 Hook；已安装 DLL／Mod 包与分发文件 SHA-256 一致，正式示例二进制不包含 SELFTEST 标记。

## 验证边界

未完整验证：实际电路内所有运行入口、战役通关、新元件注册、其他游戏构建、所有显卡和窗口配置、多种第三方原生插件组合。安全模式已实现，但未进行物理按键实机测试。

原生插件具有游戏进程权限，访问违规可能导致游戏崩溃。插件启停需要重启，不承诺安全热卸载。此版本按二进制哈希严格限制兼容范围。


